CREATE TABLE tests (
  name VARCHAR(64) PRIMARY KEY,
  added_by VARCHAR(64),
  uploaded TIMESTAMP,
  last_modified TIMESTAMP
);

CREATE TABLE segments (
  name SERIAL PRIMARY KEY,
  test_name VARCHAR(64) REFERENCES tests(name) ON DELETE CASCADE,
  added_by VARCHAR(64),
  file_path VARCHAR(64),
  uploaded TIMESTAMP
);
